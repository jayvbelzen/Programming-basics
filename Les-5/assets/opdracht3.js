let numberSequence = new Array (2, 4, 8, 9, 12, 13);
let storeNumber = 0;

for(i = 0; i < numberSequence.length; i++)
{
    storeNumber = storeNumber + numberSequence[i];
    console.log(storeNumber);
}
